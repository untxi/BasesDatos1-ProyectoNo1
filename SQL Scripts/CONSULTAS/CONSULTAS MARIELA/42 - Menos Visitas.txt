CREATE OR REPLACE FUNCTION get_MenosVisitas()
       RETURN NUMBER
       AS
              vPersona NUMBER(9);
       BEGIN 

	 SELECT MIN(PERSONA_ID) FROM BITACORA_VISIT
         WHERE BITACORA_VISIT.PERSONA_ID > AVG(PERSONA_ID);

       RETURN vPersona;

       END;
