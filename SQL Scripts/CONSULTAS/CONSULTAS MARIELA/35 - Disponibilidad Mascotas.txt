CREATE OR REPLACE FUNCTION get_Mascotas()
       RETURN NUMBER
       AS
              vPersona NUMBER(9);
       BEGIN 

	 SELECT persona_id FROM PERSONA
         WHERE PERSONA.DISPONIBLE_MASCOTA = 1;

       RETURN vPersona;

       END;
       

